spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/ms-email
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    addresses: amqps://duck.lmq.cloudamqp.com
    password: e3ws_ephf9PnM9yHozMellwU6AmpHepc
    virtual-host: oahxupxa
    username: oahxupxa

server:
  port: 8080
